# sourceGroundedAnswerGate
The system will only return answers whose every factual claim is explicitly supported by the user-provided sources. If any claim cannot be verified, the system will refuse to answer.
